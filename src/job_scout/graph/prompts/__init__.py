"""Prompt constants — the source of truth, mirrored to the Opik prompt library.

These prompts are intentionally kept minimal (clear instructions + correct output
schema). The ranking prompt in particular is the target of the Phase 3 optimizer
and should not be hand-tuned with few-shot or chain-of-thought scaffolding.
"""
